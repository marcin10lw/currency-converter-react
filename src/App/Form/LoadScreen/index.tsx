import { StyledLoadScreen } from "./styled";

const LoadScreen = () => (
  <StyledLoadScreen>
    <p>
      The currency exchange rates from the European Central Bank are being
      loaded...
    </p>
  </StyledLoadScreen>
);

export default LoadScreen;
